# PLANTILLA PÚBLICA: NO escribas tus claves en ESTE archivo (se sube a GitHub).
# Cópialo como .env (ese archivo no se sube) y pon las claves solo allí.

# --- Binance Demo Trading (https://demo.binance.com -> API Management) ---
# Permisos: SOLO lectura y trading. NUNCA habilitar retiros.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# --- Anthropic (Fase 4) ---
ANTHROPIC_API_KEY=

# --- Telegram (Fase 5) ---
# Se obtiene creando un bot con @BotFather; el número de teléfono NO sirve para la API.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Candado de dinero real ---
# Dejar vacío. Solo se rellena cuando el dueño lo autorice explícitamente (ver README).
MODO_REAL_AUTORIZADO=

# --- Panel web ---
# Clave para entrar al panel. Obligatoria si el panel es accesible desde fuera de tu PC (VPS).
PANEL_CLAVE=
